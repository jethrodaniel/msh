#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
# require "msh"

require "pry"
Pry.start

# require "irb"
# IRB.start(__FILE__)

require "msh/lexer"
require "msh/parser"
lex = Msh::Lexer.new "cowsay"
parser = Msh::Parser.new lex.tokens
interpreter = Msh::Interpreter.new parser.parse

