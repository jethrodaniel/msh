#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "msh"

require "pry"
Pry.start

# require "irb"
# IRB.start(__FILE__)

require "msh/lexer"
require "msh/parser"
L = Msh::Lexer.new "cowsay"
P = Msh::Parser.new lex.tokens
I = Msh::Interpreter.new parser.parse

n = AST::Node.new :INTERP, [], :line => 2, column: 3

require 'pry';require 'pry-byebug';binding.pry;nil
puts


